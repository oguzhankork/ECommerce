@model List<SupplierViewModelAdmin>
@{
    ViewData["Title"] = "Active";
    Layout = "~/Areas/Administrator/Views/Shared/_AdministratorLayout.cshtml";
}

<div class="mb-2">
    <table class="table table-hover text-nowrap">
        <thead>
            <tr>
                <th>#</th>
                <th>Company Name</th>
                <th>Adress</th>
                <th>Contact Name</th>
                <th>Phone Number</th>
                <th>Status</th>
                <th>Is Active</th>
                <th></th>
                <th><a href="/Administrator/Supplier/Index" class="btn btn-dark btn-md">Hepsi</a></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.SupplierId</td>
                    <td>@item.CompanyName</td>
                    <td>@item.Address</td>
                    <td>@item.ContactName</td>
                    <td>@item.PhoneNumber</td>
                    <td>
                        @switch (item.Status)
                        {
                            case ECommerce.Model.Enums.DataStatus.INSERTED:
                                <span>Eklenmiş</span>
                                break;
                            case ECommerce.Model.Enums.DataStatus.UPDATED:
                                <span>Güncellenmiş</span>
                                break;
                            case ECommerce.Model.Enums.DataStatus.DELETED:
                                <span>Silinmiş</span>
                                break;

                        }
                    </td>
                    <td>
                        @{
                            string result = item.IsActive ? "Aktif" : "Pasif";
                            <span> @result </span>
                        }
                    </td>
                    <td>
                        <a href="~/Administrator/Supplier/Update/@item.SupplierId" class="btn btn-success btn-sm">Update</a>
                        <a href="~/Administrator/Supplier/Delete/@item.SupplierId" class="btn btn-danger btn-sm">Delete</a>
                    </td>
                </tr>

            }

        </tbody>
    </table>
</div>

